server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # Optional: API proxy (VERY useful later)
    location /api/ {
        proxy_pass http://shipment-service:8081/api/;
    }
}